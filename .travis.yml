language: node_js
stages:
  - lint
addons:
  chrome: stable
jobs:
  include:
    - stage: lint
      os: linux
      dist: xenial
      install: 
        - cd js-backend
        - npm install
      script:
        - npm run lint
    - stage: lint
      os: linux
      dist: xenial
      install: 
        - cd vue-ui
        - npm install
      script:
        - npm run lint